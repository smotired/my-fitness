import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.whatinthesamhill.fitness',
  appName: 'My Fitness',
  webDir: 'dist'
};

export default config;
